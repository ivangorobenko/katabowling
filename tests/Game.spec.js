const expect = require('chai').expect;
const Game = require('../app/Game.js');

describe('Bowling', () => {
    context('Expect', () => {
        it('doit ', () => {
            expect("").to.equal(1)
        });
    })
})
